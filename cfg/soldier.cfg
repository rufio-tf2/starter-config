exec reset // Leave at the top

// Remove the "//" slashes from the lines below to enable them:
// alias +rocketJump "+jump; +duck; +attack;"
// alias -rocketJump "-jump; -duck; -attack;"
// bind MOUSE4 +rocketJump
